package com.design_patterns.behavioral.observer;

/**
 * Definition
 * The pattern is formally defined as a one to many dependency between objects
 * so that when one object changes state all the dependents are notified.
 */
public class Client {

}
